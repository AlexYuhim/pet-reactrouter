import { characters } from './fake.api/characters';
import { episodes } from './fake.api/episode';
import { locations } from './fake.api/location';

export const API = {
  characters,
  episodes,
  locations,
};
